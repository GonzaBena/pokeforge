---
import { typeColor } from "../lib/typeColors";

interface Props {
  type: string;
}

const { type } = Astro.props;
---

<span class="type-badge" style={`--badge-bg: ${typeColor(type)}`}>{type}</span>
