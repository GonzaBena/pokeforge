---
import { getTranslations, type Locale } from "../lib/i18n/translations";
const currentLocale = (Astro.currentLocale ?? "en") as Locale;
const t = getTranslations(currentLocale);
---

<div class="modal-overlay" data-detail-overlay hidden>
  <div class="modal-panel detail-modal-panel">
    <div class="modal-panel__header">
      <h2>{currentLocale === "es" ? "Detalle del Pokémon" : "Pokémon Details"}</h2>
      <button class="modal-panel__close" type="button" data-detail-close aria-label={t.modal.close}>
        <i data-lucide="x"></i>
      </button>
    </div>
    <div class="detail-modal__body" data-detail-body></div>
    <div class="detail-fab-container" data-detail-fab-container hidden>
      <div class="detail-toc-menu" data-detail-toc-menu aria-hidden="true">
        <div class="detail-toc-menu__header">
          <span>{t.modal.sectionIndex}</span>
        </div>
        <div class="detail-toc-menu__list" data-detail-toc-list></div>
      </div>
      <button
        class="detail-fab-btn"
        type="button"
        data-detail-fab-btn
        aria-label={t.modal.sectionIndex}
        title={t.modal.sectionIndex}
        aria-expanded="false"
      >
        <i data-lucide="list"></i>
      </button>
    </div>
  </div>
</div>
