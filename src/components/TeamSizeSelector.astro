---
---

<div class="team-size-selector" data-team-size-selector>
  {[1, 2, 3, 4, 5].map((n) => (
    <button class="team-size-selector__btn" type="button" data-size={n} aria-pressed="false">{n}</button>
  ))}
</div>
